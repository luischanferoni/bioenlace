# Fase 3 — MutationExecutor + handlers dominio

**Estado:** pendiente

- `MutationExecutor` + `EditMutationAuthorizationService`
- Handlers `scalar_group` (Services existentes)
- Handler `open_ui` (delegar ui_action)
- Auditoría de cambios
