# Intents — consultas / lectura compleja

Flows de lectura cuando la complejidad no cabe en `data-access-config` (info/list por atributo).

Permiso: intent o `permission` en YAML. Atributos expuestos en lectura simple siguen en `data-access-config` (`*.read`, `*.info`).
