# Ai / Application

Metadata de IA de plataforma (`clinical-text-ia.yaml`, `ai-cost-reference.yaml`).

Los prompts los edita solo el humano (regla Cursor). Carga: `ProductMetadataPaths::aiApplicationDir()`.
