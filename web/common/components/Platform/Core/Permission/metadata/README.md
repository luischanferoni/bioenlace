# Permission / metadata

Composición auth del motor (`domain-operation-policies.yaml`, `capabilities/`).

Implementaciones de cada policy id viven en PHP de dominio; este YAML solo cablea operation → policy ids.

Carga: `ProductMetadataPaths::permissionDir()`.
