name: shared
description: Librería compartida con código común para apps paciente y médico
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  local_auth: ^2.1.7
  shared_preferences: ^2.2.2
  google_fonts: ^6.2.1
  didit_sdk: ^3.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

