# EntryPoints (legacy path)

- **Chat del asistente:** `../Chat/` (`ChatOrchestrator`, canales, routing).
- **Dominio clínico:** `../../Clinical/Assistant/` (captura, motivos consulta).

La carpeta `EntryPoints/` se mantiene solo como referencia histórica en documentación externa.
