{
  "kind": "ui_definition",
  "ui_type": "picker",
  "ui_meta": {
    "picker": {
      "selection": { "mode": "single", "requires_confirmation": true },
      "chips": [],
      "list": { "item_kind": "servicio", "id_field": "id", "label_field": "name" }
    },
    "assistant_embed": { "presentation": "inline" }
  },
  "wizard_config": {
    "title": "Elegir servicio",
    "steps": [
      { "id": "pick", "title": "Servicio", "fields": ["id_servicio"] }
    ],
    "fields": [
      {
        "name": "id_servicio",
        "label": "Servicio",
        "type": "select",
        "required": true,
        "options": "{{options}}",
        "option_config": { "source": "servicios" }
      }
    ]
  }
}

