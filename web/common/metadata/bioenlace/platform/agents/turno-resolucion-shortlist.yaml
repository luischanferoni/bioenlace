# Shortlist scoreado al entrar en resolución (agente A01 v1 D1).
# {@see TurnoResolucionShortlistAgent}
version: 1
agent_id: turno-resolucion-shortlist

max_options: 3
candidate_pool_max: 40
search_max_dias: 14

scoring:
  same_pes: 30
  neighbor_option: 25
  same_date_as_original: 15
  proximity_per_day: 2
  max_proximity_days: 14

push:
  body_suffix_template: " Opciones sugeridas: {{options_summary}}"
