# Application / Flows

Manifiestos de intents del BC (`intents/{create,read,update,delete}/`).

Durante la migración DDD: los YAML pueden seguir en `common/metadata/bioenlace/<bc>/intents/` (legacy).
Discovery une ambas raíces (`IntentSchemaPaths`). Preferir esta carpeta al mover.

Ver [ddd-bounded-contexts-capas-y-metadata.md](../../../../../../docs/decisions/ddd-bounded-contexts-capas-y-metadata.md).
