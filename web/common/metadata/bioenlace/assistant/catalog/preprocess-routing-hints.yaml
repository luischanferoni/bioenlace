# Tipo: catalog
# Propósito: routing_hint del preprocess (1ª IA) — id → texto para el prompt.
# Consumidor: PreprocessRoutingHintCatalog / ChatPreprocessService.
# No poner aquí: alias, mapas legacy user_goal→hint, tags extra (van en el loader PHP).
version: 1

hints:
  clara: Hay un trámite, artículo o respuesta fija concreta a ejecutar o abrir (match 100%)
  incompletas: Pregunta de reglas, políticas, cómo funciona, o hace falta dato HIS para responder
  dudosa: Saludo solo, sin sentido útil, o falta un dato clave para actuar
  fuera_de_his: Tema ajeno al HIS
