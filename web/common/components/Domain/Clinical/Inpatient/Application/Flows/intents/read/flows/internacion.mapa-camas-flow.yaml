intent_id: internacion.mapa-camas-flow
version: 1
action_name: 'Mapa de camas'
rbac_route: /api/home/panel

initial: ver_mapa
states:
  ver_mapa:
    label: Mapa de camas del efector
    description: 'Mapa de camas del centro'
    meta:
      provides:
        - draft.id_cama
      open_ui:
        action_id: clinical.internacion.mapa-camas
    type: final

