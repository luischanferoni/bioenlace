# Fase 02 — Person UseCases

## Objetivo

Intenciones de Identity / FrontDesk en `Application/UseCase/`.

## Checklist

- [x] `RegisterPerson` ← `RegistroService`
- [x] `RegisterPatientByStaff` ← `RegistroStaffPacienteService`
- [x] `UpdateBasicIdentity` ← `PersonaIdentidadBasicaUpdateService`
- [x] `EstablishFrontDeskSession` ← `FrontDeskSessionService`
- [x] Callers API / admin / Representation / DataAccess edit handler

## Notas

Docs producto (`registro-paciente.md`, admision plan) aún mencionan nombres viejos — fase 04.
