# Fase 07 — Enlaces en el repositorio

## Objetivo

`grep` limpio de rutas `web/docs/` obsoletas en PHP, rules, README de código.

## Tareas

- [ ] `web/docs/Turnos` vs `turnos` unificado en documentación
- [ ] `PacienteController` y similares
- [ ] `.cursor/rules` si citan paths viejos

## DoD

- CI manual: cero hits a paths de archivos eliminados en fase 01.
