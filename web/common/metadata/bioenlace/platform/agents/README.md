# Agentes — políticas operativas (knobs)

Un YAML por `agent_id` (`{agent_id}.yaml`). Carga: `ProductMetadataPaths::agentFile()` / `AutonomousAgentMetadata`.

**Tipo:** knob. Umbrales y flags; **no** gates hard ni integridad clínica (dominio Yii).

Ver tipología en el [README padre](../README.md) y narrativa en `web/docs/producto/agentes-autonomos.md`.
