# Tipo: knob
# Propósito: umbrales de certeza del hilo + flag force_ambiguous.
# Consumidor: AssistantThreadStateService / ProductMetadataPaths::threadStateFile()
# Mapas goal→tag e ignore lists: constantes PHP en AssistantThreadStateService.
version: 1

certainty:
  symptom_confidence: 0.85
  followup_delta: 0.15
  cta_threshold: 0.7
  confidence_min: 0.0
  confidence_max: 1.0

force_ambiguous: true
